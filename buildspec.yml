version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build started on `date`
      - mvn package -DskipTests=true
artifacts:
  files:
    - SimpleApiSample/target/SimpleApiSample-0.0.1-SNAPSHOT.jar